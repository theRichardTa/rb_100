# create a method so that both booleans return as true

def assign_name(name = 'Bob')
  name
end


puts assign_name('Kevin') == 'Kevin'
puts assign_name == 'Bob'

# good.. not fully comfortable with default parameters/arguments but we did it on our own